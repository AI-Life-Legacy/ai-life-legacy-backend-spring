spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_life?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useUnicode=true
    username: root
    password: 11111111
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 개발환경이면 update 추천
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false

jwt:
  issuer: ai-life-legacy
  secretKey: ai-life-legacy-secret-key-must-be-at-least-256-bits-long-for-security
